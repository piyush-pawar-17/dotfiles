# greetd

This config starts a custom ReGreet build in a minimal Hyprland session.

Install it with:

```sh
sudo bash greetd/install.sh
reboot
```

The installer builds the pinned ReGreet fork, installs `greetd` and
`greetd-regreet`, and enables greetd. It does not restart greetd in the
current session.
